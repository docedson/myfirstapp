using System;

/*This name space reresents the file folder that
the class is in. */
namespace classwork
{
    //Class is a blueprint of the object Program
    class Program
    {/*This is a return type having only one item
    and a method name with an array. */
        static void Main(string[] args)
        {//This is a class and its method with a parameter
            Console.WriteLine("Hello World!");
            //The following brackets close off everything within themselves
        }
    }
}
